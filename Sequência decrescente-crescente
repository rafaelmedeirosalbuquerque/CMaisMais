/* AJEITAR
Sequência decrescente-crescente
Implemente um programa que receba como entrada um número inteiro positivo N. O programa deve imprimir a sequência N...321234...N.
*/

#include <iostream>
#include <cmath>
#include <iomanip>
#include <cstring>
#include <cctype>

using namespace std;

int main(){
    
    int N, /* inteiro positivo dado (a ser lido) */ 
    Npositivo;
    cin >> N;

    while (N < 0){
    if (N>0)
    N = Npositivo;
    }
 
}
    cout << N << endl;
    cout << Npositivo << endl;
    return 0;
}


