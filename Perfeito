/* 
Perfeito

Um número é perfeito se ele é igual à soma dos seus divisores. 
Por exemplo, o número 6 é perfeito pois 1 + 2 + 3 = 6. Escreva uma programa que lê um número inteiro e imprime se ele é perfeito ou não.
*/

#include <iostream>

using namespace std;

int main(){
    int n, aux, i;
    cin >> n;
    aux = 0;

    for(i=1; i<n; i++){     //inicia em 1, i deve ser menor do que n, acrescenta +1 ao i
        if (n%i==0){        //se resto da divisão de n por i der 0 o aux recebe i
            aux = aux + i;  //aux recebe i + o valor anterior
        }        
    }                       //fim do for
        if (aux == n){           //se aux for igual ao valor o digito é perfeito
            cout << n << " é perfeito"; 
        }else{
            cout << n << " não é perfeito";
        }  
    return 0;
    }
