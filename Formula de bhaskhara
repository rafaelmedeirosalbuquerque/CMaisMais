/*
Leia 3 valores reais (A, B e C), correspondentes a uma equação de segundo grau, e efetue o cálculo das raízes da equação de Bhaskara e imprima. 
Se não for possível calcular as raízes, mostre a mensagem correspondente “Impossivel calcular”, caso haja uma divisão por 0 ou raiz de numero negativo. 
Já quando for possível, imprima o valor da primeira raiz (soma), utilize o “\n” para pular uma linha e imprima o valor da segunda raiz (subtração).
DICA: Observe que a saída de seu programa deve ter cinco casas decimais, para isso, utilize o comando toFixed(), caso use JavaScript, como no exemplo: x = x.toFixed(5)
*/

  
//caso haja uma divisão por 0 ou raiz de numero negativo. 
//imprima o valor da primeira raiz (soma), utilize o “\n” para pular uma linha e 
//imprima o valor da segunda raiz (subtração).
//DICA: Observe que a saída de seu programa deve ter cinco casas decimais, para isso, utilize o comando toFixed(), 

#include <iostream>
#include <cmath>
#include <iomanip>

using namespace std;

int main(){
        float  A, B, C, delta, raiz1, raiz2;
    
    cin >> A >> B >> C;
    
    delta = (B*B) - (4*A*C);
    raiz1 = (-B+sqrt(delta))/(2*A);
    raiz2 = (-B-sqrt(delta))/(2*A);
    
    if(A == 0 || (delta<0)){
        cout << "Impossivel calcular\n";
    }else if (delta==0){
        cout << "Raiz 1: " << raiz1 <<endl;
        cout << "Raiz 2: " << raiz2 <<endl;
    }else{
        cout << fixed << setprecision(5) << raiz1 << endl;
        cout << fixed << setprecision(5) << raiz2 << endl;
    }
    return 0;
}  
